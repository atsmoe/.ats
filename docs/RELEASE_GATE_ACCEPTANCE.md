# T01 / T06 发布门禁验收

日期：2026-09-17。任务书基线：`main` / V2.8.23 / `e36e343`。候选：V2.8.24。仅执行批次 A；T02～T05 未实施。

## 状态

- T01：实施完成，待验收。
- T06：说明同步完成，待验收数据回填。
- 正式部署：尚未执行。
- 提交 / PR：待生成。

## 验收记录

| 编号 | 要证明的行为 | 当前证据 / 结果 |
|------|--------------|-----------------|
| CI-01 | 正常提交依次构建、Node、浏览器、上传与部署 | 待远端验证 |
| CI-02 | 重要浏览器用例失败阻止部署 | 待安全分支演练；禁止生产故障注入 |
| CI-03 | 安装失败、启动失败、取消不会继续部署 | 待安全分支演练 |
| CI-04 | 普通 PR 不发布，手动发布使用同一门禁 | 配置已核对，待运行验证 |
| CI-05 | 可下载 trace / screenshot / report 并定位 SHA / project | 已配置 HTML / JSON、失败截图与 trace；待下载验证 |
| CI-06 | deployment id 与 page_url 输出引用一致 | 4 项配置检查通过，待实际部署输出 |
| CI-07 | 测试提交、产物与部署一致 | 浏览器检查前后全量 SHA-256 清单比对；待正常发布运行证据 |

## 安全演练约束

在独立 `codex/` 测试分支复用候选 build 步骤与 `needs: build`，注入明确标注的故障；下游仅输出模拟标记，无 Pages 权限或部署动作。对照候选与演练配置的差异并保留运行链接。模拟标记被跳过只证明等价依赖链阻断，不宣称生产网站经历过故障发布。

## 本地与覆盖边界

- 针对性发布配置检查：4/4 通过；此类静态检查不能替代 GitHub Actions 行为。
- 完整生产构建与 Chromium 桌面/手机视口回归：待执行。
- CI 使用一次失败重试；分别报告首次失败、重试通过和最终失败。
- 安装失败可能没有浏览器报告；取消会尝试上传已有报告，但 runner 强制终止时不保证完整。Actions 运行日志是此类情况的最低证据。
- 不宣称 Firefox、WebKit、实体手机、OS 级输入法或 bfcache 已验收。
- 不改变仓库保护规则，不修改电脑配置；无新增依赖、数据或媒体改写。
