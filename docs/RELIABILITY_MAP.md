# 完善与可靠性决策地图

目标：在不改变现有内容和视觉体验的前提下，让项目更快、更稳定、更容易由单人长期维护。

默认原则：

- 用户负责产品方向与内容取舍；技术方案、风险控制和验证由维护代理负责。
- 每次只处理一个可验证的小目标，完成构建与必要测试后再继续。
- 优先修复代码与文档已经明确冲突的部分，不把大规模重构作为起点。
- 不因优化删除源数据、原始素材或用户内容；涉及取舍时先说明影响。
- 动画、交互和视觉表现是网站展示技术力的核心，不以简单删减特效换取性能；通过独立加载、自适应质量和帧预算保证流畅。
- 视觉方向采用原创的深色工业、终端界面、几何切割和世界主题色体系，可参考鹰角网络作品的设计方法，但不复制其素材、布局或品牌识别元素。
- 每次推送到 GitHub 必须同时维护面向访客的更新日志和面向维护的开发日志，并由 CI 强制检查。
- 面向用户的汇报只包含结果、可见影响、风险和下一步，不要求理解代码。

## #1：建立可靠性基线

Blocked by: 无

Type: Discuss

### Question

当前项目哪些核心能力必须持续可用，并用什么自动检查证明它们没有被改坏？

### Answer

已建立首版基线：生产构建会验证 6 个公开页面、3 个世界数据、全局事件索引、首页与世界页脚本隔离、时间线/分支/弹窗关键容器、生产数据引用的媒体文件，以及 JS 体积预算。

当前基线：世界页 bundle 由 551.5 KiB 降至 37.4 KiB；星图 bundle 为 517.1 KiB；构建索引包含 1427 个事件。真实浏览器冒烟自动化仍待后续在允许本地浏览器访问的环境中补充。

## #2：修复页面代码重复打包

Blocked by: #1

Type: Prototype

### Question

如何让首页只加载 Three.js 星图代码，世界页只加载时间线代码，同时保持现有页面行为？

### Answer

已完成。新增星图专用入口，首页只加载包含 Three.js 的 `star-map-3d.js`，世界页加载不含 Three.js 的 `bundle.js`；生产测试会阻止两者再次混入。

## #3：建立最小自动化测试保护网

Blocked by: #1, #2

Type: Prototype

### Question

哪些少量测试能够以最低维护成本阻止数据、导航和时间线功能回归？

### Answer

部分完成。已增加零额外依赖的 Node 构建产物测试，并接入 `npm run build`。浏览器冒烟流程仍待补充。

## #3A：建立炫酷但稳定的视觉与动画基准

Blocked by: #2, #3

Type: Prototype

### Question

如何以原创方式强化深色工业、终端信息层级、几何切割和世界主题视觉，同时让桌面与移动设备上的动画都保持流畅？

### Answer

待完成。先建立动画帧预算、设备性能分级和关键交互清单，再制作可切换的视觉原型；不直接复制参考品牌的素材或页面。

## #4：统一时间线 Engine 与 UI 边界

Blocked by: #3

Type: Prototype

### Question

如何消除 `virtual-timeline.js` 与 `timeline-ui.js` 的重复渲染职责，并符合现有 GUIDELINES？

### Answer

待完成。目标是 Engine 只计算可见范围和位置，UI 成为事件卡片与交互的唯一渲染者。

## #5：治理媒体资源与部署体积

Blocked by: #1, #3

Type: Prototype

### Question

如何减少未引用图片、GIF 和重复产物，同时保证所有现有内容仍能显示并保留可恢复的源素材？

### Answer

待完成。先建立引用清单和缺失资源检查，再决定是否只复制引用资源；不直接删除源素材。

## #6：让核心文档与实现重新一致

Blocked by: #2, #4, #5

Type: Discuss

### Question

如何划分 README、PRD、ARCHITECTURE、GUIDELINES 和开发日志的职责，并移除失效描述？

### Answer

待完成。最终需要明确文档权威顺序，并将包体积等易过期数字改为自动检查或带版本的快照。

## #7：评估数据是否需要按分支拆分

Blocked by: #1, #3

Type: Prototype

### Question

FF14 等世界数据继续增长时，单文件加载是否已影响真实体验，是否值得改变当前“每世界单 JSON”的架构？

### Answer

待完成。仅在测量证明下载或解析成为瓶颈后实施；否则维持简单的单文件架构。
